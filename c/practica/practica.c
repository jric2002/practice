#include <stdio.h>
#include <string.h>
int main(int argc, char *argv[]) {
  char value[128];
  if (value[0] = '\0') {
    printf("esta vacio");
  } else {
    printf("NO esta vacio");
  }
  return 0;
}