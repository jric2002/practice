#include <iostream>
using namespace std;
int main() {
  //c
  int cont = 2;
  int num = 3;
  cont += 5*num + 4;
  cout << cont << endl;
  //c
  int a = 60;
  int b = 13;
  int c = a&b;
  cout << c << endl;
  //c
  int nu = 300;
  char sexo = 'F';
  bool pivot = nu > 100 && sexo == 'M';
  bool p = true;
  bool q = false;
  //c
  int result;
  //result = (b*sqrt(a*a - x*x))/sqrt(a*a*b*b + x*x(a*a - b*b));
  cout << sizeof(b) << endl;
  return 0;
}